apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openproject
  namespace: openproject
spec:
  interval: 30m0s
  timeout: 15m0s
  chart:
    spec:
      chart: openproject
      sourceRef:
        kind: HelmRepository
        name: openproject
        namespace: flux-system
  values:
    develop: true
    image:
      tag: "17"
    openproject:
      https: true
      host: openproject.cloud.clearclean.local
      seed_locale: de
    ingress:
      enabled: true
      ingressClassName: traefik
      host: openproject.cloud.clearclean.local
      annotations:
        cert-manager.io/cluster-issuer: clearclean-ca
      tls:
        enabled: true
        secretName: openproject-tls
    persistence:
      enabled: true
      storageClassName: local-path
      accessModes:
        - ReadWriteOnce
    postgresql:
      primary:
        persistence:
          storageClass: local-path
