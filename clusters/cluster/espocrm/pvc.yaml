apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: espocrm-pvc
  namespace: espocrm
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
